# Repeading — 開発計画

YouTube の字幕を取り込み、一文ずつ再生して「聞く → 真似る」を回すリピーティング練習アプリ。

## 1. 前提と方針

| 項目 | 決定 |
| --- | --- |
| 練習形式 | リピーティング（一文聞いて、無音の間に真似る）。**録音・発音採点は作らない** |
| 字幕の入手 | 貼り付け（YouTube 文字起こしパネル）＋ SRT / VTT ファイル |
| 対象環境 | PC ブラウザ中心。キーボード操作前提 |
| 「真似る」間 | 自動で無音を挟む。手動でも進める |
| 構成 | 完全クライアントサイド（サーバーなし） |

### 字幕の入手方法をなぜ「貼り付け」にしたか

YouTube Data API の `captions.download` は**動画所有者の OAuth が必要**で、他人の動画の字幕は取得できない。
非公式の `timedtext` エンドポイントや `yt-dlp` はサーバーが必要なうえ、仕様変更で頻繁に壊れ、IP ブロックの対象にもなる。

一方、動画そのものは **YouTube IFrame Player API** で正規に埋め込んで再生できる。
そこで「再生は公式プレーヤー、字幕はユーザーが手元から持ち込む」という形にした。
サーバー費用ゼロ、壊れる外部依存ゼロで成立する。自動取得（P4）は後から足せるよう、
パーサの入口を 1 箇所（`parseSubtitles`）に絞ってある。

## 2. データフロー

```
YouTube URL ──→ extractVideoId ──────────────→ videoId
                                                  │
字幕テキスト ──→ parseSubtitles ──→ Cue[] ──→ segmentCues ──→ Sentence[]
 (貼付/ファイル)    形式を自動判定      重複除去・文結合          │
                                                                 ▼
                                    usePractice ⇄ useYouTubePlayer (IFrame API)
                                      状態機械        区間再生・自動停止
```

主要な型（`src/lib/types.ts`）:

```ts
interface Cue      { start: number; end: number; text: string }
interface Sentence { id: number; start: number; end: number; text: string }
```

## 3. 各パーツの設計

### 3.1 字幕パーサ (`src/lib/parseSubtitles.ts`)

入力の形式を判定して `Cue[]` に正規化する。ユーザーは「自分が何形式を持っているか」を
意識しなくてよい。

| 形式 | 判定条件 |
| --- | --- |
| `srt` | `-->` を含み、ミリ秒がカンマ区切り |
| `vtt` | `-->` を含み、`WEBVTT` ヘッダまたはピリオド区切り |
| `youtube-transcript` | `0:12` 形式で始まる行が全体の 1/4 以上 |
| `plain-text` | 上記以外（タイムスタンプなし＝区間再生は不可） |

文字起こしパネルの貼り付けは「`0:12 本文`」と「`0:12` 改行 `本文`」の両方が発生するため、
どちらも受ける。最終 cue には終了時刻が無いので、文字数（14 字/秒）から見積もる。

本文の整形では VTT のインラインタグ、ASS の位置指定、`>>`（話者交代）、
`[Music]` のような非発話表記、HTML エンティティを落とす。

### 3.2 文分割エンジン (`src/lib/segment.ts`) — 品質の肝

YouTube の自動字幕は **2〜4 語ずつ・句読点なし・前の行を繰り返しながら流れる**ため、
cue をそのまま練習単位にすると使い物にならない。2 段階で処理する。

**(a) ローリング重複の除去** — 直近 20 語の末尾と次の cue の先頭の最長一致を探して切る。
1 語だけの一致は "very very" のような正当な繰り返しと区別できないので、cue 全体が重複している場合を除き認めない。
語単位の時刻は持っていないため、落とした語数の比率で開始時刻を按分する。

**(b) 文への結合** — 次のいずれかで切る。

- 句読点で終わる（略語 `Mr.` と頭文字 `J.` は除外）
- 無音ギャップがしきい値以上
- 累積 80 文字 / 8 秒を超える

句読点の有無で戦略を切り替えるのが要点。句読点付きの字幕では、文中の自然な間で切れないよう
ギャップのしきい値を 0.6 秒 → 1.5 秒に引き上げる。句読点が無い自動字幕では
ギャップと長さ上限だけが頼りになる。

自動分割は必ず外すので、**P3 で手直し UI（ここで分割 / 前と結合）を入れる**。

### 3.3 区間再生 (`src/lib/useYouTubePlayer.ts`)

IFrame Player API に「ここまで再生して止まる」機能は無いので、
`seekTo` → `playVideo` の後に `getCurrentTime()` を 50ms 間隔でポーリングし、
終端で `pauseVideo()` する。実測の誤差は ±100ms 程度。

2 つの落とし穴に対処している。

- **頭切れ・語尾切れ** — 開始を 0.08 秒手前、終端を 0.12 秒後ろにずらす。
- **シーク完了前の誤判定** — `seekTo` 直後の `getCurrentTime()` は古い位置を返す。
  区間内に入ったことを確認する（armed）までは終端判定をしない。

### 3.4 練習の状態機械 (`src/lib/usePractice.ts`)

```
        ┌──────────────────────────────┐
        │                              │ 繰り返し回数が残っている
        ▼                              │
  listening ──(区間終端で自動停止)──→ mimicking ──→ 次の文 or idle
    聞く          無音を挟む             真似る
```

無音の長さ＝**文の実再生時間 × 倍率**。再生速度で体感の長さが変わるので、
`(end - start) / playbackRate` を基準に取る。

スペースキーは文脈で意味が変わる — 再生中は「停止」、無音待ち中は「もう真似し終わったので進む」、
停止中は「再生」。練習中に手が止まらないことを優先した。

### 3.5 字幕の 3 段階表示

リピーティングは字幕を見ながらでは効果が薄い。かといって全く分からないと詰まる。

`隠す` → `ヒント`（各単語の 1 文字目だけ `t·· ··· ·· ····`）→ `表示` の 3 段階を `H` キーで巡回する。

### 3.6 配色とデザイン (`src/styles.css`, `src/lib/useTheme.ts`)

Apple のサイトと iOS の UI に寄せたデザイン言語で組んでいる。OS のフォント（`-apple-system` 系）、
ピル型のアクションボタン、iOS 風のセグメンテッドコントロールとスイッチ、角丸 18px のカードと控えめな影。
Apple のロゴやフォントファイルといった素材は持ち込まず、システムフォントと配色・形状の設計だけを借りている。

配色は `:root` の CSS 変数だけで切り替わる。既定はライト（背景 `#f5f5f7`）で、
OS がダークなら `data-theme` が未指定でもダークに寄せ、手動で切り替えたときは `data-theme` が常に優先される。
ダーク側も純黒は使わず `#1d1d1f` を基調にした。

手動の選択は `localStorage` に保存し、保存された時点で OS への追従をやめる。
プライベートモードでは `localStorage` の読み書きが例外を投げうるので、失敗しても既定値で動くようにしてある。

## 4. フェーズ

| | 内容 | 状態 |
| --- | --- | --- |
| **P0** | URL 入力 → 字幕取り込み → 文分割 → 一文再生・前後移動 | ✅ 実装済み |
| **P1** | 自動ポーズ、繰り返し回数、速度、キーボード、字幕 3 段階 | ✅ 実装済み |
| **P2** | IndexedDB 保存、教材一覧、進捗の記憶、苦手文ブックマーク、復習キュー | 未着手 |
| **P3** | 分割の手動編集 UI、設定の永続化、文ごとのループ、区間の微調整 | 未着手 |
| **P4** | 字幕の自動取得（サーバー / ブラウザ拡張）、Whisper 対応、翻訳、語彙抽出 | 未着手 |

### P2 で決めること

保存単位は「教材 = 動画 ID + 文リスト + 設定 + 進捗」。
IndexedDB に置き、トップ画面を教材一覧にする。文ごとに `難しい` フラグを立てられるようにして、
フラグの付いた文だけを流す復習モードに繋げる。

### P4 の選択肢

- **ブラウザ拡張** — ユーザーのブラウザで YouTube のページから字幕を読む。サーバー不要で、
  「URL を入れるだけ」に最も近い体験になる。拡張の審査と保守が要る。
- **サーバー (yt-dlp)** — 実装は最短だが、壊れやすく運用コストと ToS リスクを抱える。
- **Whisper** — 字幕が無い動画も対象にできる。音声の取り出しが別途必要。

## 5. 技術構成

- Vite + React 18 + TypeScript（strict）
- 状態は React のフックのみ。状態管理ライブラリは入れていない
- スタイルは手書き CSS 1 枚
- テストは Vitest。パーサと文分割エンジンにロジックが集中しているので、そこを重点的に
- デプロイは静的ホスティング（`base: './'` で設置場所を選ばない）

## 6. 既知の制約

- タイムスタンプの無いベタテキストは取り込めるが、区間再生はできない（警告を出して練習開始を止める）
- 語単位の時刻を持たないので、重複除去後の開始時刻は近似
- 年齢制限・埋め込み禁止の動画は IFrame Player で再生できない
- 自動分割の精度は字幕の質に依存する（P3 の手直し UI で補う)
